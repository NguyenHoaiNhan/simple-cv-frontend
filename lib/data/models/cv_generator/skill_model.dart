class SkillModel {
  const SkillModel({
    this.skillId,
    this.sectionTitle,
    this.skillName,
    this.level
  });

  final int? skillId;
  final String? sectionTitle;
  final String? skillName;
  final int? level;
}
